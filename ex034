s = float(input('Qual o salário do funcionário? '))
if s > 1250:
    sa = s * (110/100)
    print(f'O salário de R${s:.2f} com o aumento de 10% ficará {sa:.2f}.')
else:
    sa = s * (115/100)
    print(f'O salário de R${s:.2f} com o aumento de 15% ficará {sa:.2f}.')
